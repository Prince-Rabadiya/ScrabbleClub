%h5 Games
%table{class: 'table'}
  %thead
    %th{scope: 'col'} Name
    %th{scope: 'col'} Description
    %th{scope: 'col'} Start at
    %th{scope: 'col'} End at
    %th{colspan: '3', scope: 'col'}Actions

  %tbody
    - @games.each do |game|
      %tr
        %td
          = game.name
        %td
          = game.description
        %td
          = game.start_at
        %td
          = game.end_at
        %td
          = link_to :show, game
        %td
          = link_to :edit, edit_game_path(game.id)
        %td
          = link_to :destroy, game, method: :delete, data: { confirm: 'Are you sure?' }
        
= link_to 'Create game', new_game_path
